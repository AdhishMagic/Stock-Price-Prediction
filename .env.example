## Frontend Environment Variables
# API base URL (normalized automatically if malformed). Include protocol.
VITE_API_BASE_URL=http://localhost:8000

# Enable the stock analysis sidebar (ModelAccuracyMetrics on the right).
# Accepts: 1, true, yes, on (case-insensitive) to enable.
VITE_ENABLE_STOCK_SIDEBAR=false

## Backend / Training Environment Variables
# Force synthetic data mode (no network) for both training & inference.
OFFLINE_MODE=1

# Auto-training tickers & horizons when backend container starts (comma separated)
BOOTSTRAP_TICKERS=MSFT,AAPL
BOOTSTRAP_HORIZONS=5,10

# (Add any secrets like API keys below; do NOT commit real values)
# SOME_API_KEY=

# Copy this file to .env (frontend) or export variables manually for backend.